apiVersion: v1
kind: Service
metadata:
  name: account-svc-service
  labels:
    app: account-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: account-service

